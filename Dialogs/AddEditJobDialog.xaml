<Window x:Class="OneManVan.Dialogs.AddEditJobDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Job" Height="750" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        ResizeMode="CanResize"
        MinHeight="650" MinWidth="600">

    <Window.Resources>
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Margin" Value="0,15,0,10"/>
        </Style>
        
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource SubtextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        
        <Style x:Key="FormTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Surface2Brush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}"/>
            <Setter Property="SelectionBrush" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource OverlayBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FormComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Surface2Brush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style x:Key="FormDatePicker" TargetType="DatePicker">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Surface2Brush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" Padding="20,15">
            <StackPanel>
                <TextBlock x:Name="HeaderTitle" Text="Add Job" FontSize="18" FontWeight="Bold" 
                           Foreground="{DynamicResource TextBrush}"/>
                <TextBlock Text="Create a new job/service call" FontSize="12" 
                           Foreground="{DynamicResource SubtextBrush}" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Form Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20,10,20,10">
            <StackPanel>
                
                <!-- Basic Information -->
                <TextBlock Text="Basic Information" Style="{StaticResource SectionHeader}"/>

                <!-- Title -->
                <TextBlock Text="Job Title *" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="TitleTextBox" Style="{StaticResource FormTextBox}" 
                         Margin="0,0,0,12" MaxLength="200"/>

                <!-- Customer -->
                <TextBlock Text="Customer *" Style="{StaticResource FieldLabel}"/>
                <ComboBox x:Name="CustomerCombo" Style="{StaticResource FormComboBox}" 
                          Margin="0,0,0,12" SelectionChanged="OnCustomerChanged"/>

                <!-- Site -->
                <TextBlock Text="Site/Location" Style="{StaticResource FieldLabel}"/>
                <ComboBox x:Name="SiteCombo" Style="{StaticResource FormComboBox}" 
                          Margin="0,0,0,12" SelectionChanged="OnSiteChanged"/>

                <!-- Asset -->
                <TextBlock Text="Asset/Equipment" Style="{StaticResource FieldLabel}"/>
                <ComboBox x:Name="AssetCombo" Style="{StaticResource FormComboBox}" 
                          Margin="0,0,0,12"/>

                <!-- Job Type & Priority -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Job Type *" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="JobTypeCombo" Style="{StaticResource FormComboBox}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Priority *" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="PriorityCombo" Style="{StaticResource FormComboBox}"/>
                    </StackPanel>
                </Grid>

                <!-- Status & Job Number -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Status *" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="StatusCombo" Style="{StaticResource FormComboBox}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Job Number" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="JobNumberTextBox" Style="{StaticResource FormTextBox}" 
                                 MaxLength="50" IsEnabled="False"/>
                    </StackPanel>
                </Grid>

                <!-- Description -->
                <TextBlock Text="Description" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="DescriptionTextBox" Style="{StaticResource FormTextBox}" 
                         AcceptsReturn="True" TextWrapping="Wrap" Height="80" 
                         VerticalScrollBarVisibility="Auto" Margin="0,0,0,12" MaxLength="2000"/>

                <!-- Scheduling -->
                <TextBlock Text="Scheduling" Style="{StaticResource SectionHeader}"/>

                <!-- Scheduled Date & Time -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Scheduled Date" Style="{StaticResource FieldLabel}"/>
                        <DatePicker x:Name="ScheduledDatePicker" Style="{StaticResource FormDatePicker}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Start Time" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="StartTimeTextBox" Style="{StaticResource FormTextBox}" 
                                 MaxLength="10"/>
                    </StackPanel>
                </Grid>

                <!-- Duration & End Time -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Duration (minutes)" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="DurationTextBox" Style="{StaticResource FormTextBox}" 
                                 MaxLength="4" TextChanged="OnDurationChanged"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="End Time" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="EndTimeTextBox" Style="{StaticResource FormTextBox}" 
                                 MaxLength="10" IsEnabled="False"/>
                    </StackPanel>
                </Grid>

                <!-- Financial -->
                <TextBlock Text="Financial" Style="{StaticResource SectionHeader}"/>

                <!-- Estimated Total & Paid Amount -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Estimated Total" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="EstimatedTotalTextBox" Style="{StaticResource FormTextBox}" 
                                 MaxLength="10"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Actual Total" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="ActualTotalTextBox" Style="{StaticResource FormTextBox}" 
                                 MaxLength="10"/>
                    </StackPanel>
                </Grid>

            </StackPanel>
        </ScrollViewer>

        <!-- Footer Buttons -->
        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" 
                BorderThickness="0,1,0,0" BorderBrush="{DynamicResource Surface2Brush}" 
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="2" Content="Cancel" Width="100" Height="36"
                        Background="{DynamicResource Surface2Brush}"
                        Foreground="{DynamicResource TextBrush}"
                        BorderThickness="0" Cursor="Hand" Click="OnCancelClick">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="6" Padding="15,8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button x:Name="SaveButton" Grid.Column="4" Content="Create Job" Width="120" Height="36"
                        Background="{DynamicResource PrimaryBrush}"
                        Foreground="White" FontWeight="SemiBold"
                        BorderThickness="0" Cursor="Hand" Click="OnSaveClick">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="6" Padding="15,8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
