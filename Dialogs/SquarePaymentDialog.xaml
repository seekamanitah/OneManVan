<Window x:Class="OneManVan.Dialogs.SquarePaymentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Square Payment" 
        Height="400" Width="400"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" Padding="20,15">
            <StackPanel>
                <TextBlock Text="Process Payment" FontSize="18" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextBrush}"/>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <TextBlock Text="Amount: " FontSize="14" 
                               Foreground="{DynamicResource SubtextBrush}"/>
                    <TextBlock x:Name="AmountText" FontSize="14" FontWeight="Bold"
                               Foreground="{DynamicResource SecondaryBrush}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Payment Info -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
            
            <TextBlock x:Name="StatusText" Text="Ready to process payment" 
                       FontSize="14" Foreground="{DynamicResource TextBrush}"
                       HorizontalAlignment="Center" Margin="0,15,0,0" TextAlignment="Center"
                       TextWrapping="Wrap" MaxWidth="300"/>
            <TextBlock Text="Note: Full Square Web Payments SDK integration requires WebView2."
                       FontSize="11" Foreground="{DynamicResource SubtextBrush}"
                       HorizontalAlignment="Center" Margin="0,10,0,0" TextAlignment="Center"
                       TextWrapping="Wrap" MaxWidth="300"/>
        </StackPanel>

        <!-- Footer -->
        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="&#x1F512;" FontSize="12" Margin="0,0,5,0" Opacity="0.7"/>
                    <TextBlock Text="Secured by Square" FontSize="11" 
                               Foreground="{DynamicResource SubtextBrush}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="ProcessButton" Content="Simulate Payment" Click="Process_Click"
                            Background="{DynamicResource PrimaryBrush}"
                            Foreground="{DynamicResource BackgroundBrush}"
                            Padding="15,8" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6" 
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button x:Name="CancelButton" Content="Cancel" Click="Cancel_Click"
                            Background="{DynamicResource Surface2Brush}"
                            Foreground="{DynamicResource TextBrush}"
                            Padding="15,8" BorderThickness="0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6" 
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
