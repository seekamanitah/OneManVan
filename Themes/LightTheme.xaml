<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Catppuccin Latte Light Theme -->
    
    <!-- Base Colors -->
    <Color x:Key="BackgroundColor">#eff1f5</Color>
    <Color x:Key="SurfaceColor">#e6e9ef</Color>
    <Color x:Key="Surface2Color">#ccd0da</Color>
    <Color x:Key="OverlayColor">#9ca0b0</Color>
    
    <!-- Text Colors -->
    <Color x:Key="TextColor">#4c4f69</Color>
    <Color x:Key="SubtextColor">#5c5f77</Color>
    <Color x:Key="Subtext2Color">#6c6f85</Color>
    
    <!-- Accent Colors -->
    <Color x:Key="PrimaryColor">#1e66f5</Color>
    <Color x:Key="SecondaryColor">#40a02b</Color>
    <Color x:Key="WarningColor">#c4710b</Color>
    <Color x:Key="ErrorColor">#d20f39</Color>
    <Color x:Key="InfoColor">#179299</Color>
    
    <!-- Brushes -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="Surface2Brush" Color="{StaticResource Surface2Color}"/>
    <SolidColorBrush x:Key="OverlayBrush" Color="{StaticResource OverlayColor}"/>
    
    <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="SubtextBrush" Color="{StaticResource SubtextColor}"/>
    <SolidColorBrush x:Key="Subtext2Brush" Color="{StaticResource Subtext2Color}"/>
    
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
    <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
    <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource InfoColor}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#fe640b"/>
    <SolidColorBrush x:Key="LavenderBrush" Color="#7287fd"/>
    <SolidColorBrush x:Key="Surface3Brush" Color="#bcc0cc"/>
    
    <!-- Control-specific brushes -->
    <SolidColorBrush x:Key="ButtonBackgroundBrush" Color="{StaticResource PrimaryColor}"/>
    <SolidColorBrush x:Key="ButtonForegroundBrush" Color="#ffffff"/>
    <SolidColorBrush x:Key="ButtonHoverBrush" Color="#3574f7"/>
    
    <SolidColorBrush x:Key="InputBackgroundBrush" Color="#ffffff"/>
    <SolidColorBrush x:Key="InputBorderBrush" Color="{StaticResource Surface2Color}"/>
    <SolidColorBrush x:Key="InputFocusBorderBrush" Color="{StaticResource PrimaryColor}"/>
    
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="#ffffff"/>
    <SolidColorBrush x:Key="CardBorderBrush" Color="{StaticResource Surface2Color}"/>
    
    <SolidColorBrush x:Key="SidebarBackgroundBrush" Color="#dce0e8"/>
    <SolidColorBrush x:Key="SidebarItemHoverBrush" Color="{StaticResource Surface2Color}"/>
    <SolidColorBrush x:Key="SidebarItemSelectedBrush" Color="{StaticResource SurfaceColor}"/>
    
    <!-- List/Selection Colors -->
    <SolidColorBrush x:Key="ListItemHoverBrush" Color="#d5d8e0"/>
    <SolidColorBrush x:Key="ListItemSelectedBrush" Color="{StaticResource Surface2Color}"/>
    <SolidColorBrush x:Key="ListItemSelectedHoverBrush" Color="#c0c4d0"/>
    <SolidColorBrush x:Key="TreeItemSelectedBrush" Color="#251e66f5"/>
    
    <!-- Badge/Tag Colors (dark text on colored backgrounds in light mode) -->
    <SolidColorBrush x:Key="BadgeTextBrush" Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="ButtonDarkTextBrush" Color="#ffffff"/>
    
    <!-- Status Badge Backgrounds with better contrast -->
    <SolidColorBrush x:Key="BadgeDefaultBrush" Color="#ccd0da"/>
    <SolidColorBrush x:Key="BadgeSuccessBrush" Color="#6040a02b"/>
    <SolidColorBrush x:Key="BadgeWarningBrush" Color="#50c4710b"/>
    <SolidColorBrush x:Key="BadgeErrorBrush" Color="#50d20f39"/>
    <SolidColorBrush x:Key="BadgeInfoBrush" Color="#501e66f5"/>
    
    <!-- Theme Identifier -->
    <system:String x:Key="ThemeName" xmlns:system="clr-namespace:System;assembly=mscorlib">Light</system:String>

    <!-- ComboBox Item Style for Light Mode -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd" 
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            BorderThickness="0">
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Surface2Brush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Surface2Brush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox Toggle Button Template -->
    <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>
            <Border x:Name="Border"
                    Grid.ColumnSpan="2"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource Surface2Brush}"
                    BorderThickness="1"
                    CornerRadius="6"/>
            <Path x:Name="Arrow"
                  Grid.Column="1"
                  Fill="{StaticResource SubtextBrush}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Data="M 0 0 L 4 4 L 8 0 Z"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource OverlayBrush}"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Full ComboBox Style for Light Mode -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Surface2Brush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                      Template="{StaticResource ComboBoxToggleButton}"
                                      Focusable="false"
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"/>
                        <ContentPresenter x:Name="ContentSite"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="12,8,25,8"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"/>
                        <TextBox x:Name="PART_EditableTextBox"
                                 Style="{x:Null}"
                                 HorizontalAlignment="Left"
                                 VerticalAlignment="Center"
                                 Margin="12,8,25,8"
                                 Focusable="True"
                                 Background="Transparent"
                                 Foreground="{StaticResource TextBrush}"
                                 CaretBrush="{StaticResource TextBrush}"
                                 Visibility="Hidden"
                                 IsReadOnly="{TemplateBinding IsReadOnly}"/>
                        <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Grid x:Name="DropDown"
                                  SnapsToDevicePixels="True"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border x:Name="DropDownBorder"
                                        Background="{StaticResource SurfaceBrush}"
                                        BorderBrush="{StaticResource Surface2Brush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Margin="0,2,0,0">
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                        </Trigger>
                        <Trigger Property="IsGrouping" Value="True">
                            <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
                        </Trigger>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter Property="IsTabStop" Value="False"/>
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
