<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Catppuccin Mocha Dark Theme -->
    
    <!-- Base Colors -->
    <Color x:Key="BackgroundColor">#1e1e2e</Color>
    <Color x:Key="SurfaceColor">#313244</Color>
    <Color x:Key="Surface2Color">#45475a</Color>
    <Color x:Key="OverlayColor">#6c7086</Color>
    
    <!-- Text Colors -->
    <Color x:Key="TextColor">#cdd6f4</Color>
    <Color x:Key="SubtextColor">#a6adc8</Color>
    <Color x:Key="Subtext2Color">#9399b2</Color>
    
    <!-- Accent Colors -->
    <Color x:Key="PrimaryColor">#89b4fa</Color>
    <Color x:Key="SecondaryColor">#a6e3a1</Color>
    <Color x:Key="WarningColor">#f9e2af</Color>
    <Color x:Key="ErrorColor">#f38ba8</Color>
    <Color x:Key="InfoColor">#94e2d5</Color>
    
    <!-- Brushes -->
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="Surface2Brush" Color="{StaticResource Surface2Color}"/>
    <SolidColorBrush x:Key="OverlayBrush" Color="{StaticResource OverlayColor}"/>
    
    <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="SubtextBrush" Color="{StaticResource SubtextColor}"/>
    <SolidColorBrush x:Key="Subtext2Brush" Color="{StaticResource Subtext2Color}"/>
    
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
    <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
    <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource InfoColor}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#fab387"/>
    <SolidColorBrush x:Key="LavenderBrush" Color="#b4befe"/>
    <SolidColorBrush x:Key="Surface3Brush" Color="#585b70"/>
    
    <!-- Control-specific brushes -->
    <SolidColorBrush x:Key="ButtonBackgroundBrush" Color="{StaticResource PrimaryColor}"/>
    <SolidColorBrush x:Key="ButtonForegroundBrush" Color="#1e1e2e"/>
    <SolidColorBrush x:Key="ButtonHoverBrush" Color="#7aa8f0"/>
    
    <SolidColorBrush x:Key="InputBackgroundBrush" Color="{StaticResource Surface2Color}"/>
    <SolidColorBrush x:Key="InputBorderBrush" Color="{StaticResource OverlayColor}"/>
    <SolidColorBrush x:Key="InputFocusBorderBrush" Color="{StaticResource PrimaryColor}"/>
    
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="CardBorderBrush" Color="{StaticResource Surface2Color}"/>
    
    <SolidColorBrush x:Key="SidebarBackgroundBrush" Color="#181825"/>
    <SolidColorBrush x:Key="SidebarItemHoverBrush" Color="{StaticResource Surface2Color}"/>
    <SolidColorBrush x:Key="SidebarItemSelectedBrush" Color="{StaticResource SurfaceColor}"/>
    
    <!-- List/Selection Colors -->
    <SolidColorBrush x:Key="ListItemHoverBrush" Color="#3a3a4d"/>
    <SolidColorBrush x:Key="ListItemSelectedBrush" Color="{StaticResource Surface2Color}"/>
    <SolidColorBrush x:Key="ListItemSelectedHoverBrush" Color="#50526b"/>
    <SolidColorBrush x:Key="TreeItemSelectedBrush" Color="#2589b4fa"/>
    
    <!-- Badge/Tag Colors (light text on colored backgrounds in dark mode) -->
    <SolidColorBrush x:Key="BadgeTextBrush" Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="ButtonDarkTextBrush" Color="#1e1e2e"/>
    
    <!-- Status Badge Backgrounds with better contrast -->
    <SolidColorBrush x:Key="BadgeDefaultBrush" Color="#45475a"/>
    <SolidColorBrush x:Key="BadgeSuccessBrush" Color="#40a6e3a1"/>
    <SolidColorBrush x:Key="BadgeWarningBrush" Color="#40f9e2af"/>
    <SolidColorBrush x:Key="BadgeErrorBrush" Color="#40f38ba8"/>
    <SolidColorBrush x:Key="BadgeInfoBrush" Color="#4089b4fa"/>
    
    <!-- Theme Identifier -->
    <system:String x:Key="ThemeName" xmlns:system="clr-namespace:System;assembly=mscorlib">Dark</system:String>

    <!-- ComboBox Item Style for Dark Mode -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd" 
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            BorderThickness="0">
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Surface2Brush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource BackgroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Surface2Brush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox Toggle Button Template -->
    <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>
            <Border x:Name="Border"
                    Grid.ColumnSpan="2"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource Surface2Brush}"
                    BorderThickness="1"
                    CornerRadius="6"/>
            <Path x:Name="Arrow"
                  Grid.Column="1"
                  Fill="{StaticResource SubtextBrush}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Data="M 0 0 L 4 4 L 8 0 Z"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource OverlayBrush}"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Full ComboBox Style for Dark Mode -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Surface2Brush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                      Template="{StaticResource ComboBoxToggleButton}"
                                      Focusable="false"
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"/>
                        <ContentPresenter x:Name="ContentSite"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="12,8,25,8"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"/>
                        <TextBox x:Name="PART_EditableTextBox"
                                 Style="{x:Null}"
                                 HorizontalAlignment="Left"
                                 VerticalAlignment="Center"
                                 Margin="12,8,25,8"
                                 Focusable="True"
                                 Background="Transparent"
                                 Foreground="{StaticResource TextBrush}"
                                 CaretBrush="{StaticResource TextBrush}"
                                 Visibility="Hidden"
                                 IsReadOnly="{TemplateBinding IsReadOnly}"/>
                        <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Grid x:Name="DropDown"
                                  SnapsToDevicePixels="True"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border x:Name="DropDownBorder"
                                        Background="{StaticResource SurfaceBrush}"
                                        BorderBrush="{StaticResource Surface2Brush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Margin="0,2,0,0">
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                        </Trigger>
                        <Trigger Property="IsGrouping" Value="True">
                            <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
                        </Trigger>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter Property="IsTabStop" Value="False"/>
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
