<UserControl x:Class="OneManVan.Pages.AssetDataGridPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OneManVan.Pages"
             xmlns:converters="clr-namespace:OneManVan.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:EquipmentTypeColorConverter x:Key="EquipmentTypeColor"/>
        <converters:RefrigerantColorConverter x:Key="RefrigerantColor"/>
        <converters:WarrantyStatusColorConverter x:Key="WarrantyStatusColor"/>
        
        <!-- DataGrid Style -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource Surface2Brush}"/>
            <Setter Property="RowBackground" Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource Surface2Brush}"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        
        <!-- DataGrid Header Style -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Surface2Brush}"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>
        
        <!-- DataGrid Cell Style -->
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ListItemSelectedBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- DataGrid Row Style with warranty highlighting -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ListItemHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ListItemSelectedBrush}"/>
                </Trigger>
                <!-- Warranty expired - subtle red tint -->
                <DataTrigger Binding="{Binding IsWarrantyExpired}" Value="True">
                    <Setter Property="Background" Value="#10F44336"/>
                </DataTrigger>
                <!-- Warranty expiring soon - subtle orange tint -->
                <DataTrigger Binding="{Binding IsWarrantyExpiringSoon}" Value="True">
                    <Setter Property="Background" Value="#10FF9800"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- Filter Chip Button Style -->
        <Style x:Key="FilterChip" TargetType="ToggleButton">
            <Setter Property="Background" Value="{DynamicResource Surface2Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="14" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Action Button Style -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="{DynamicResource Surface2Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Asset Management" FontSize="24" FontWeight="Bold" 
                               Foreground="{DynamicResource TextBrush}"/>
                </StackPanel>
                <TextBlock x:Name="AssetCountText" Text="Loading assets..." 
                           FontSize="13" Foreground="{DynamicResource SubtextBrush}" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Export" Click="OnExportClick"
                        Style="{StaticResource SecondaryButton}" Margin="0,0,10,0"/>
                <Button Content="+ Add Asset" Click="OnAddAssetClick"
                        Style="{StaticResource ActionButton}"/>
            </StackPanel>
        </Grid>

        <!-- Search and Filters Row -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Search Box -->
            <Border Grid.Column="0" Background="{DynamicResource SurfaceBrush}" 
                    CornerRadius="8" Padding="2">
                <Grid>
                    <TextBox x:Name="SearchBox" 
                             Background="Transparent"
                             Foreground="{DynamicResource TextBrush}"
                             BorderThickness="0"
                             Padding="12,10"
                             FontSize="13"
                             TextChanged="OnSearchTextChanged"/>
                    <TextBlock Text="Search assets..." 
                               IsHitTestVisible="False"
                               Foreground="{DynamicResource SubtextBrush}"
                               FontSize="13"
                               VerticalAlignment="Center" Margin="14,0,0,0"
                               x:Name="SearchPlaceholder"/>
                </Grid>
            </Border>

            <!-- Filter Chips -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="15,0,0,0">
                <TextBlock Text="Status:" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                           VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ToggleButton x:Name="FilterAll" Content="All" IsChecked="True"
                              Style="{StaticResource FilterChip}" Margin="0,0,5,0"
                              Click="OnWarrantyFilterClick"/>
                <ToggleButton x:Name="FilterValid" Content="Valid"
                              Style="{StaticResource FilterChip}" Margin="0,0,5,0"
                              Click="OnWarrantyFilterClick"/>
                <ToggleButton x:Name="FilterExpiring" Content="Expiring"
                              Style="{StaticResource FilterChip}" Margin="0,0,5,0"
                              Click="OnWarrantyFilterClick"/>
                <ToggleButton x:Name="FilterExpired" Content="Expired"
                              Style="{StaticResource FilterChip}" Margin="0,0,5,0"
                              Click="OnWarrantyFilterClick"/>
                <ToggleButton x:Name="FilterR22" Content="R-22"
                              Style="{StaticResource FilterChip}" Margin="0,0,15,0"
                              Click="OnWarrantyFilterClick"/>
                
                <TextBlock Text="Type:" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                           VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="EquipmentFilter" Width="150" SelectionChanged="OnEquipmentFilterChanged">
                    <ComboBoxItem Content="All Equipment" IsSelected="True"/>
                    <ComboBoxItem Content="Furnace"/>
                    <ComboBoxItem Content="Air Conditioner"/>
                    <ComboBoxItem Content="Heat Pump"/>
                    <ComboBoxItem Content="Mini Split"/>
                    <ComboBoxItem Content="Thermostat"/>
                </ComboBox>
            </StackPanel>
        </Grid>

        <!-- Quick Stats Bar -->
        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" 
                CornerRadius="8" Padding="15,10" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <StackPanel Orientation="Horizontal" Margin="0,0,30,0">
                    <TextBlock Text="Total:" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="TotalCount" Text="0" FontSize="16" FontWeight="Bold"
                               Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,30,0">
                    <TextBlock Text="Valid Warranty:" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="ValidCount" Text="0" FontSize="16" FontWeight="Bold"
                               Foreground="{DynamicResource SuccessBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,30,0">
                    <TextBlock Text="Expiring Soon:" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="ExpiringCount" Text="0" FontSize="16" FontWeight="Bold"
                               Foreground="{DynamicResource WarningBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,30,0">
                    <TextBlock Text="Expired:" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="ExpiredCount" Text="0" FontSize="16" FontWeight="Bold"
                               Foreground="{DynamicResource ErrorBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="R-22 Units:" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="R22Count" Text="0" FontSize="16" FontWeight="Bold"
                               Foreground="#C62828" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <Border Grid.Row="3" Background="{DynamicResource SurfaceBrush}" 
                CornerRadius="8" Padding="0">
            <DataGrid x:Name="AssetGrid" 
                      Style="{StaticResource ModernDataGrid}"
                      MouseDoubleClick="OnRowDoubleClick"
                      SelectionChanged="OnAssetSelected"
                      Sorting="OnDataGridSorting">
                <DataGrid.Columns>
                    <!-- Equipment Type Icon -->
                    <DataGridTemplateColumn Header="" Width="40" CanUserSort="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Width="28" Height="28" CornerRadius="14"
                                        Background="{Binding EquipmentType, Converter={StaticResource EquipmentTypeColor}}">
                                    <TextBlock Text="{Binding EquipmentTypeIcon}" FontSize="14"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Serial Number -->
                    <DataGridTemplateColumn Header="Serial" Width="130" SortMemberPath="Serial">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Serial}" FontWeight="Medium"
                                               Foreground="{DynamicResource TextBrush}"/>
                                    <TextBlock Text="{Binding AssetTag}" FontSize="10"
                                               Foreground="{DynamicResource SubtextBrush}"
                                               Visibility="{Binding AssetTag, Converter={StaticResource BoolToVisibility}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Brand/Model -->
                    <DataGridTemplateColumn Header="Equipment" Width="180" SortMemberPath="Brand">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock FontWeight="Medium" Foreground="{DynamicResource TextBrush}">
                                        <Run Text="{Binding Brand}"/>
                                        <Run Text=" "/>
                                        <Run Text="{Binding Model}"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding Nickname}" FontSize="11" FontStyle="Italic"
                                               Foreground="{DynamicResource SubtextBrush}"
                                               Visibility="{Binding Nickname, Converter={StaticResource BoolToVisibility}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Customer -->
                    <DataGridTextColumn Header="Customer" Binding="{Binding Customer.Name}" Width="140">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <!-- Capacity -->
                    <DataGridTemplateColumn Header="Capacity" Width="100" SortMemberPath="BtuRating">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding CapacityDisplay}" FontSize="12"
                                               Foreground="{DynamicResource TextBrush}"/>
                                    <TextBlock Text="{Binding EfficiencyDisplay}" FontSize="10"
                                               Foreground="{DynamicResource SubtextBrush}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Refrigerant -->
                    <DataGridTemplateColumn Header="Refrigerant" Width="90" SortMemberPath="RefrigerantType">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="6,3"
                                        Background="{Binding RefrigerantType, Converter={StaticResource RefrigerantColor}}">
                                    <TextBlock Text="{Binding RefrigerantDisplay}" FontSize="11"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Age -->
                    <DataGridTextColumn Header="Age" Binding="{Binding AgeYears}" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="{DynamicResource SubtextBrush}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <!-- Warranty Status -->
                    <DataGridTemplateColumn Header="Warranty" Width="110" SortMemberPath="DaysUntilWarrantyExpires">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="8,4">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{DynamicResource BadgeSuccessBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsWarrantyExpired}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource BadgeErrorBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsWarrantyExpiringSoon}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource BadgeWarningBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding WarrantyEndDate}" Value="{x:Null}">
                                                    <Setter Property="Background" Value="{DynamicResource Surface2Brush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding WarrantyStatusDisplay}" FontSize="11" FontWeight="Medium"
                                               HorizontalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{DynamicResource BackgroundBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding WarrantyEndDate}" Value="{x:Null}">
                                                        <Setter Property="Foreground" Value="{DynamicResource SubtextBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Last Service -->
                    <DataGridTextColumn Header="Last Service" 
                                        Binding="{Binding LastServiceDate, StringFormat='{}{0:MMM d}'}" 
                                        Width="85">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{DynamicResource SubtextBrush}"/>
                                <Setter Property="FontSize" Value="11"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <!-- Actions -->
                    <DataGridTemplateColumn Header="Actions" Width="100" CanUserSort="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="View" ToolTip="View Details"
                                            Click="OnViewClick" Tag="{Binding}"
                                            Background="Transparent" BorderThickness="0"
                                            Foreground="{DynamicResource TextBrush}"
                                            FontSize="12" Padding="6,4" Cursor="Hand"/>
                                    <Button Content="Edit" ToolTip="Edit Asset"
                                            Click="OnEditClick" Tag="{Binding}"
                                            Background="Transparent" BorderThickness="0"
                                            Foreground="{DynamicResource PrimaryBrush}"
                                            FontSize="12" Padding="6,4" Cursor="Hand"/>
                                    <Button Content="Delete" ToolTip="Delete Asset"
                                            Click="OnDeleteClick" Tag="{Binding}"
                                            Background="Transparent" BorderThickness="0"
                                            Foreground="{DynamicResource ErrorBrush}"
                                            FontSize="12" Padding="6,4" Cursor="Hand"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Pagination Footer -->
        <Border Grid.Row="4" Background="{DynamicResource SurfaceBrush}" 
                CornerRadius="8" Padding="15,10" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Items per page -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Show" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox x:Name="PageSizeCombo" Width="70" SelectionChanged="OnPageSizeChanged">
                        <ComboBoxItem Content="25"/>
                        <ComboBoxItem Content="50" IsSelected="True"/>
                        <ComboBoxItem Content="100"/>
                        <ComboBoxItem Content="All"/>
                    </ComboBox>
                    <TextBlock Text="entries" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                               VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>

                <!-- Page info -->
                <TextBlock Grid.Column="1" x:Name="PageInfo" Text="Showing 1-50 of 100 assets" 
                           FontSize="12" Foreground="{DynamicResource SubtextBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <!-- Page navigation -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="FirstPageBtn" Content="|&lt;" Click="OnFirstPageClick"
                            Style="{StaticResource SecondaryButton}" Padding="8,4" Margin="0,0,5,0"/>
                    <Button x:Name="PrevPageBtn" Content="&lt;" Click="OnPrevPageClick"
                            Style="{StaticResource SecondaryButton}" Padding="8,4" Margin="0,0,5,0"/>
                    <TextBlock x:Name="CurrentPageText" Text="Page 1" FontSize="12" 
                               Foreground="{DynamicResource TextBrush}"
                               VerticalAlignment="Center" Margin="10,0"/>
                    <Button x:Name="NextPageBtn" Content="&gt;" Click="OnNextPageClick"
                            Style="{StaticResource SecondaryButton}" Padding="8,4" Margin="0,0,5,0"/>
                    <Button x:Name="LastPageBtn" Content="&gt;|" Click="OnLastPageClick"
                            Style="{StaticResource SecondaryButton}" Padding="8,4"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="5" Background="#80000000" CornerRadius="8"
                x:Name="LoadingOverlay" Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Loading assets..." FontSize="14" Foreground="White"
                           HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
