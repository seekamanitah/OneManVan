<UserControl x:Class="OneManVan.Pages.CustomerDataGridPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OneManVan.Pages"
             xmlns:converters="clr-namespace:OneManVan.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:CustomerStatusColorConverter x:Key="CustomerStatusColor"/>
        <converters:CustomerTypeColorConverter x:Key="CustomerTypeColor"/>
        
        <!-- DataGrid Style -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource Surface2Brush}"/>
            <Setter Property="RowBackground" Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource Surface2Brush}"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        
        <!-- DataGrid Header Style -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Surface2Brush}"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>
        
        <!-- DataGrid Cell Style -->
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ListItemSelectedBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- DataGrid Row Style -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ListItemHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ListItemSelectedBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Filter Chip Button Style -->
        <Style x:Key="FilterChip" TargetType="ToggleButton">
            <Setter Property="Background" Value="{DynamicResource Surface2Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="14" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Action Button Style -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="{DynamicResource Surface2Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="Customer Management" FontSize="24" FontWeight="Bold" 
                               Foreground="{DynamicResource TextBrush}"/>
                </StackPanel>
                <TextBlock x:Name="CustomerCountText" Text="Loading customers..." 
                           FontSize="13" Foreground="{DynamicResource SubtextBrush}" Margin="34,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="?? Export" Click="OnExportClick"
                        Style="{StaticResource SecondaryButton}" Margin="0,0,10,0"/>
                <Button Content="+ Add Customer" Click="OnAddCustomerClick"
                        Style="{StaticResource ActionButton}"/>
            </StackPanel>
        </Grid>

        <!-- Search and Filters Row -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Search Box -->
            <Border Grid.Column="0" Background="{DynamicResource SurfaceBrush}" 
                    CornerRadius="8" Padding="2">
                <Grid>
                    <TextBox x:Name="SearchBox" 
                             Background="Transparent"
                             Foreground="{DynamicResource TextBrush}"
                             BorderThickness="0"
                             Padding="12,10"
                             FontSize="13"
                             TextChanged="OnSearchTextChanged"/>
                    <TextBlock Text="?? Search customers..." 
                               IsHitTestVisible="False"
                               Foreground="{DynamicResource SubtextBrush}"
                               FontSize="13"
                               VerticalAlignment="Center" Margin="14,0,0,0"
                               x:Name="SearchPlaceholder"/>
                </Grid>
            </Border>

            <!-- Filter Chips -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="15,0,0,0">
                <TextBlock Text="Status:" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                           VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ToggleButton x:Name="FilterAll" Content="All" IsChecked="True"
                              Style="{StaticResource FilterChip}" Margin="0,0,5,0"
                              Click="OnStatusFilterClick"/>
                <ToggleButton x:Name="FilterActive" Content="? Active"
                              Style="{StaticResource FilterChip}" Margin="0,0,5,0"
                              Click="OnStatusFilterClick"/>
                <ToggleButton x:Name="FilterVIP" Content="?? VIP"
                              Style="{StaticResource FilterChip}" Margin="0,0,5,0"
                              Click="OnStatusFilterClick"/>
                <ToggleButton x:Name="FilterLead" Content="? Lead"
                              Style="{StaticResource FilterChip}" Margin="0,0,5,0"
                              Click="OnStatusFilterClick"/>
                <ToggleButton x:Name="FilterDelinquent" Content="?? Delinquent"
                              Style="{StaticResource FilterChip}" Margin="0,0,15,0"
                              Click="OnStatusFilterClick"/>
                
                <TextBlock Text="Type:" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                           VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="TypeFilter" Width="150" SelectionChanged="OnTypeFilterChanged">
                    <ComboBoxItem Content="All Types" IsSelected="True"/>
                    <ComboBoxItem Content="?? Residential"/>
                    <ComboBoxItem Content="?? Commercial"/>
                    <ComboBoxItem Content="??? Property Manager"/>
                    <ComboBoxItem Content="??? Government"/>
                </ComboBox>
            </StackPanel>
        </Grid>

        <!-- Quick Stats Bar -->
        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" 
                CornerRadius="8" Padding="15,10" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <StackPanel Orientation="Horizontal" Margin="0,0,30,0">
                    <TextBlock Text="Total:" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="TotalCount" Text="0" FontSize="16" FontWeight="Bold"
                               Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,30,0">
                    <TextBlock Text="Active:" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="ActiveCount" Text="0" FontSize="16" FontWeight="Bold"
                               Foreground="{DynamicResource SuccessBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,30,0">
                    <TextBlock Text="VIP:" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="VIPCount" Text="0" FontSize="16" FontWeight="Bold"
                               Foreground="#FFD700" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,30,0">
                    <TextBlock Text="Outstanding:" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="OutstandingBalance" Text="$0.00" FontSize="16" FontWeight="Bold"
                               Foreground="{DynamicResource ErrorBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Lifetime Revenue:" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="LifetimeRevenue" Text="$0.00" FontSize="16" FontWeight="Bold"
                               Foreground="{DynamicResource SecondaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <Border Grid.Row="3" Background="{DynamicResource SurfaceBrush}" 
                CornerRadius="8" Padding="0">
            <DataGrid x:Name="CustomerGrid" 
                      Style="{StaticResource ModernDataGrid}"
                      MouseDoubleClick="OnRowDoubleClick"
                      SelectionChanged="OnCustomerSelected"
                      Sorting="OnDataGridSorting">
                <DataGrid.Columns>
                    <!-- Status Icon -->
                    <DataGridTemplateColumn Header="" Width="40" CanUserSort="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Width="24" Height="24" CornerRadius="12"
                                        Background="{Binding Status, Converter={StaticResource CustomerStatusColor}}">
                                    <TextBlock Text="{Binding StatusIcon}" FontSize="12"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Customer Number -->
                    <DataGridTextColumn Header="ID" Binding="{Binding CustomerNumber}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{DynamicResource SubtextBrush}"/>
                                <Setter Property="FontSize" Value="11"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <!-- Name -->
                    <DataGridTemplateColumn Header="Customer" Width="200" SortMemberPath="Name">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextBrush}"/>
                                        <TextBlock Text="" FontSize="12" Margin="5,0,0,0"
                                                   Visibility="{Binding IsVip, Converter={StaticResource BoolToVisibility}}"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding CompanyName}" FontSize="11"
                                               Foreground="{DynamicResource SubtextBrush}"
                                               Visibility="{Binding CompanyName, Converter={StaticResource BoolToVisibility}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Type -->
                    <DataGridTemplateColumn Header="Type" Width="120" SortMemberPath="CustomerType">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="6,3"
                                        Background="{Binding CustomerType, Converter={StaticResource CustomerTypeColor}}">
                                    <TextBlock Text="{Binding CustomerTypeDisplay}" FontSize="11"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Contact -->
                    <DataGridTemplateColumn Header="Contact" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Phone}" FontSize="12"
                                               Foreground="{DynamicResource TextBrush}"/>
                                    <TextBlock Text="{Binding Email}" FontSize="11"
                                               Foreground="{DynamicResource SubtextBrush}"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Sites -->
                    <DataGridTextColumn Header="Sites" Binding="{Binding Sites.Count}" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <!-- Assets -->
                    <DataGridTextColumn Header="Assets" Binding="{Binding Assets.Count}" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <!-- Balance -->
                    <DataGridTemplateColumn Header="Balance" Width="100" SortMemberPath="AccountBalance">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding AccountBalance, StringFormat='${0:N2}'}" 
                                           FontWeight="Medium">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasOutstandingBalance}" Value="True">
                                                    <Setter Property="Foreground" Value="{DynamicResource ErrorBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Lifetime Revenue -->
                    <DataGridTextColumn Header="Revenue" Binding="{Binding LifetimeRevenue, StringFormat='${0:N2}'}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{DynamicResource SecondaryBrush}"/>
                                <Setter Property="FontWeight" Value="Medium"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <!-- Last Service -->
                    <DataGridTextColumn Header="Last Service" 
                                        Binding="{Binding LastServiceDate, StringFormat='{}{0:MMM d, yyyy}'}" 
                                        Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{DynamicResource SubtextBrush}"/>
                                <Setter Property="FontSize" Value="11"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <!-- Actions -->
                    <DataGridTemplateColumn Header="Actions" Width="120" CanUserSort="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="" ToolTip="Call Customer"
                                            Click="OnCallClick" Tag="{Binding}"
                                            Background="Transparent" BorderThickness="0"
                                            Foreground="{DynamicResource TextBrush}"
                                            FontSize="14" Padding="6,4" Cursor="Hand"/>
                                    <Button Content="?" ToolTip="Email Customer"
                                            Click="OnEmailClick" Tag="{Binding}"
                                            Background="Transparent" BorderThickness="0"
                                            Foreground="{DynamicResource TextBrush}"
                                            FontSize="14" Padding="6,4" Cursor="Hand"/>
                                    <Button Content="?" ToolTip="Edit Customer"
                                            Click="OnEditClick" Tag="{Binding}"
                                            Background="Transparent" BorderThickness="0"
                                            Foreground="{DynamicResource PrimaryBrush}"
                                            FontSize="14" Padding="6,4" Cursor="Hand"/>
                                    <Button Content="" ToolTip="Delete Customer"
                                            Click="OnDeleteClick" Tag="{Binding}"
                                            Background="Transparent" BorderThickness="0"
                                            Foreground="{DynamicResource ErrorBrush}"
                                            FontSize="14" Padding="6,4" Cursor="Hand"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Pagination Footer -->
        <Border Grid.Row="4" Background="{DynamicResource SurfaceBrush}" 
                CornerRadius="8" Padding="15,10" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Items per page -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Show" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox x:Name="PageSizeCombo" Width="70" SelectionChanged="OnPageSizeChanged">
                        <ComboBoxItem Content="25"/>
                        <ComboBoxItem Content="50" IsSelected="True"/>
                        <ComboBoxItem Content="100"/>
                        <ComboBoxItem Content="All"/>
                    </ComboBox>
                    <TextBlock Text="entries" FontSize="12" Foreground="{DynamicResource SubtextBrush}" 
                               VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>

                <!-- Page info -->
                <TextBlock Grid.Column="1" x:Name="PageInfo" Text="Showing 1-50 of 100 customers" 
                           FontSize="12" Foreground="{DynamicResource SubtextBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <!-- Page navigation -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="FirstPageBtn" Content="?" Click="OnFirstPageClick"
                            Style="{StaticResource SecondaryButton}" Padding="8,4" Margin="0,0,5,0"/>
                    <Button x:Name="PrevPageBtn" Content="?" Click="OnPrevPageClick"
                            Style="{StaticResource SecondaryButton}" Padding="8,4" Margin="0,0,5,0"/>
                    <TextBlock x:Name="CurrentPageText" Text="Page 1" FontSize="12" 
                               Foreground="{DynamicResource TextBrush}"
                               VerticalAlignment="Center" Margin="10,0"/>
                    <Button x:Name="NextPageBtn" Content="?" Click="OnNextPageClick"
                            Style="{StaticResource SecondaryButton}" Padding="8,4" Margin="0,0,5,0"/>
                    <Button x:Name="LastPageBtn" Content="?" Click="OnLastPageClick"
                            Style="{StaticResource SecondaryButton}" Padding="8,4"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="5" Background="#80000000" CornerRadius="8"
                x:Name="LoadingOverlay" Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="?" FontSize="32" HorizontalAlignment="Center"/>
                <TextBlock Text="Loading customers..." FontSize="14" Foreground="White"
                           HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
