<UserControl x:Class="OneManVan.Pages.JobListPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:OneManVan.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1100"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <converters:JobStatusColorConverter x:Key="StatusColorConverter"/>
        
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource BackgroundBrush}"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="{DynamicResource Surface2Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>

        <Style x:Key="FilterChip" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Border" 
                                Background="{DynamicResource Surface2Brush}" 
                                CornerRadius="16" 
                                Padding="14,6" 
                                Cursor="Hand">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="500"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Job List -->
        <Border Grid.Column="0" Background="{DynamicResource BackgroundBrush}" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="Jobs" FontSize="28" VerticalAlignment="Center" Margin="0,0,10,0"
                                   Foreground="{DynamicResource PrimaryBrush}"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Schedule" FontSize="24" FontWeight="Bold" 
                                       Foreground="{DynamicResource TextBrush}"/>
                            <TextBlock x:Name="JobCountText" Text="0 jobs" 
                                       FontSize="13" Foreground="{DynamicResource SubtextBrush}"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Content="+ New Job" Click="OnAddJobClick"
                                Style="{StaticResource ActionButton}" Margin="0,0,10,0"/>
                        <Button Content="Kanban View" Click="OnKanbanViewClick"
                                Style="{StaticResource SecondaryButton}"/>
                    </StackPanel>
                </Grid>

                <!-- Date Header -->
                <Border Grid.Row="1" Background="{DynamicResource PrimaryBrush}" 
                        CornerRadius="8" Padding="16,12" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock x:Name="DateHeaderText" Text="Today" 
                                       FontSize="20" FontWeight="Bold" Foreground="White"/>
                            <TextBlock x:Name="DateSubHeaderText" Text="December 12, 2024" 
                                       FontSize="13" Foreground="#BBDEFB"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Content="Previous" Click="OnPreviousDayClick"
                                    Background="#1565C0" Foreground="White" 
                                    BorderThickness="0" Padding="12,6" Margin="0,0,8,0"
                                    Cursor="Hand"/>
                            <Button Content="Today" Click="OnTodayClick"
                                    Background="#1565C0" Foreground="White" 
                                    BorderThickness="0" Padding="12,6" Margin="0,0,8,0"
                                    Cursor="Hand"/>
                            <Button Content="Next" Click="OnNextDayClick"
                                    Background="#1565C0" Foreground="White" 
                                    BorderThickness="0" Padding="12,6"
                                    Cursor="Hand"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Status Filter Chips -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,15">
                    <RadioButton x:Name="TodayFilter" Content="Today" 
                                 Style="{StaticResource FilterChip}" 
                                 IsChecked="True" Checked="OnFilterChanged"/>
                    <RadioButton x:Name="ScheduledFilter" Content="Scheduled" 
                                 Style="{StaticResource FilterChip}" Checked="OnFilterChanged"/>
                    <RadioButton x:Name="InProgressFilter" Content="In Progress" 
                                 Style="{StaticResource FilterChip}" Checked="OnFilterChanged"/>
                    <RadioButton x:Name="CompletedFilter" Content="Completed" 
                                 Style="{StaticResource FilterChip}" Checked="OnFilterChanged"/>
                    <RadioButton x:Name="AllFilter" Content="All" 
                                 Style="{StaticResource FilterChip}" Checked="OnFilterChanged"/>
                </StackPanel>

                <!-- Search Bar -->
                <TextBox Grid.Row="3" x:Name="SearchBox"
                         Text=""
                         Background="{DynamicResource SurfaceBrush}"
                         Foreground="{DynamicResource TextBrush}"
                         BorderBrush="{DynamicResource Surface2Brush}"
                         Padding="12,10" FontSize="14" Margin="0,0,0,10"
                         TextChanged="OnSearchTextChanged">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Tag" Value="Search jobs..."/>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Job List -->
                <Border Grid.Row="4" Background="{DynamicResource SurfaceBrush}" CornerRadius="8">
                    <ListView x:Name="JobListView"
                              Background="Transparent"
                              BorderThickness="0"
                              SelectionChanged="OnJobSelected"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{DynamicResource Surface2Brush}"
                                                    BorderThickness="0,0,0,1"
                                                    Padding="0">
                                                <ContentPresenter/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource ListItemSelectedBrush}"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsMouseOver" Value="True"/>
                                            <Condition Property="IsSelected" Value="False"/>
                                        </MultiTrigger.Conditions>
                                        <Setter Property="Background" Value="{DynamicResource ListItemHoverBrush}"/>
                                    </MultiTrigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="4"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Status Color Bar -->
                                    <Rectangle Grid.Column="0" Fill="{Binding Status, Converter={StaticResource StatusColorConverter}}"
                                               VerticalAlignment="Stretch"/>
                                    
                                    <!-- Job Content -->
                                    <Grid Grid.Column="1" Margin="15,12">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- Time Badge and Title -->
                                        <StackPanel Grid.Row="0" Orientation="Horizontal">
                                            <Border Background="#E3F2FD" CornerRadius="10" Padding="8,3" Margin="0,0,10,0">
                                                <TextBlock Text="{Binding ScheduledDate, StringFormat='{}{0:h:mm tt}'}"
                                                           FontSize="11" FontWeight="SemiBold" Foreground="#1976D2"/>
                                            </Border>
                                            <TextBlock Text="{Binding Title}" 
                                                       FontSize="15" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource TextBrush}"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                        
                                        <!-- Status Badge -->
                                        <Border Grid.Row="0" Grid.Column="1" 
                                                Background="{Binding Status, Converter={StaticResource StatusColorConverter}}"
                                                CornerRadius="10" Padding="8,3">
                                            <TextBlock Text="{Binding StatusDisplay}" 
                                                       FontSize="10" FontWeight="Bold" Foreground="White"/>
                                        </Border>
                                        
                                        <!-- Customer and Address -->
                                        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="0,6,0,0">
                                            <TextBlock Text="{Binding Customer.Name}" 
                                                       FontSize="13" Foreground="{DynamicResource TextBrush}"/>
                                            <TextBlock Text="{Binding Site.Address}" 
                                                       FontSize="12" Foreground="{DynamicResource SubtextBrush}"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        
                                        <!-- Job Type and Priority -->
                                        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" 
                                                    Orientation="Horizontal" Margin="0,8,0,0">
                                            <Border Background="{DynamicResource Surface2Brush}" 
                                                    CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                                                <TextBlock Text="{Binding JobTypeDisplay}" 
                                                           FontSize="10" Foreground="{DynamicResource PrimaryBrush}"/>
                                            </Border>
                                            <Border Background="{DynamicResource Surface2Brush}" 
                                                    CornerRadius="4" Padding="6,2">
                                                <TextBlock Text="{Binding PriorityDisplay}" 
                                                           FontSize="10" Foreground="{DynamicResource SubtextBrush}"/>
                                            </Border>
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>

                <!-- Loading Overlay -->
                <Border Grid.Row="4" Background="#80000000" CornerRadius="8"
                        x:Name="LoadingOverlay" Visibility="Collapsed">
                    <TextBlock Text="Loading..." FontSize="16" Foreground="White"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Empty State -->
                <Border Grid.Row="4" x:Name="EmptyState" Visibility="Collapsed"
                        Background="{DynamicResource SurfaceBrush}" CornerRadius="8">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="No jobs found" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource SubtextBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Try adjusting your filters or add a new job" 
                                   FontSize="13" Foreground="{DynamicResource SubtextBrush}"
                                   HorizontalAlignment="Center" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

    </Grid>
</UserControl>