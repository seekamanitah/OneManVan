<UserControl x:Class="OneManVan.Pages.TestRunnerPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{DynamicResource PageBackgroundBrush}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundBrush}" 
                Padding="24,16" BorderThickness="0,0,0,1" 
                BorderBrush="{DynamicResource BorderBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel>
                    <TextBlock Text="?? Test Runner" FontSize="24" FontWeight="Bold"
                               Foreground="{DynamicResource TextBrush}"/>
                    <TextBlock Text="Execute comprehensive tests for database, models, and CRUD operations"
                               FontSize="13" Foreground="{DynamicResource SubtextBrush}" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="SeedDataButton" Content="?? Seed Test Data" 
                            Click="OnSeedDataClicked" Style="{DynamicResource AccentButton}"
                            Padding="16,8" Margin="0,0,8,0"/>
                    <Button x:Name="ClearDataButton" Content="??? Clear All Data" 
                            Click="OnClearDataClicked" Style="{DynamicResource DangerButton}"
                            Padding="16,8"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Test Stage Buttons -->
        <Border Grid.Row="1" Background="{DynamicResource CardBackgroundBrush}" 
                Padding="24,16" BorderThickness="0,0,0,1" 
                BorderBrush="{DynamicResource BorderBrush}">
            <StackPanel>
                <TextBlock Text="Select Test Stage" FontSize="14" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextBrush}" Margin="0,0,0,12"/>
                
                <WrapPanel Orientation="Horizontal">
                    <Button Content="Stage 1: Database &amp; Models" Click="OnRunStage1Clicked"
                            Margin="0,0,8,8" Padding="12,8" Style="{DynamicResource SecondaryButton}"/>
                    <Button Content="Stage 2: CRUD Operations" Click="OnRunStage2Clicked"
                            Margin="0,0,8,8" Padding="12,8" Style="{DynamicResource SecondaryButton}"/>
                    <Button Content="Stage 3: UI &amp; Theme" Click="OnRunStage3Clicked"
                            Margin="0,0,8,8" Padding="12,8" Style="{DynamicResource SecondaryButton}"/>
                    <Button Content="Stage 4: ViewModels" Click="OnRunStage4Clicked"
                            Margin="0,0,8,8" Padding="12,8" Style="{DynamicResource SecondaryButton}"/>
                    <Button Content="Run All Tests" Click="OnRunAllTestsClicked"
                            Margin="0,0,8,8" Padding="12,8" Style="{DynamicResource PrimaryButton}"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- Test Results -->
        <Border Grid.Row="2" Margin="24,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Test Results" FontSize="14" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextBrush}" Margin="0,0,0,8"/>

                <Border Grid.Row="1" Background="{DynamicResource CardBackgroundBrush}"
                        CornerRadius="8" Padding="16" BorderThickness="1"
                        BorderBrush="{DynamicResource BorderBrush}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        <TextBox x:Name="ResultsTextBox" 
                                 Text="Click a test stage button to run tests..."
                                 FontFamily="Consolas" FontSize="12"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Foreground="{DynamicResource TextBrush}"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"/>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="{DynamicResource CardBackgroundBrush}" 
                Padding="24,12" BorderThickness="0,1,0,0" 
                BorderBrush="{DynamicResource BorderBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                        <TextBlock Text="? Passed: " Foreground="{DynamicResource SubtextBrush}"/>
                        <TextBlock x:Name="PassedCountLabel" Text="0" FontWeight="Bold" Foreground="#4CAF50"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                        <TextBlock Text="? Failed: " Foreground="{DynamicResource SubtextBrush}"/>
                        <TextBlock x:Name="FailedCountLabel" Text="0" FontWeight="Bold" Foreground="#F44336"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="?? Total: " Foreground="{DynamicResource SubtextBrush}"/>
                        <TextBlock x:Name="TotalCountLabel" Text="0" FontWeight="Bold" 
                                   Foreground="{DynamicResource TextBrush}"/>
                    </StackPanel>
                </StackPanel>

                <Button Grid.Column="1" Content="?? Copy Results" Click="OnCopyResultsClicked"
                        Padding="16,8" Style="{DynamicResource SecondaryButton}"/>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border x:Name="LoadingOverlay" Grid.RowSpan="4" 
                Background="#80000000" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                <TextBlock x:Name="LoadingText" Text="Running tests..." 
                           Foreground="White" FontSize="14" Margin="0,16,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
