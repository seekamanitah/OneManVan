<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OneManVan.Mobile.Controls.HeroMetricCard">
    
    <Border BackgroundColor="{AppThemeBinding Light=White, Dark=#2D2D2D}"
            StrokeShape="RoundRectangle 16"
            Stroke="Transparent"
            Padding="20,16">
        
        <Border.Shadow>
            <Shadow Brush="Black" Offset="0,2" Radius="8" Opacity="0.12"/>
        </Border.Shadow>

        <VerticalStackLayout Spacing="4">
            <!-- Metric Value -->
            <Label x:Name="ValueLabel"
                   FontSize="32"
                   FontAttributes="Bold"
                   TextColor="{AppThemeBinding Light=#2196F3, Dark=#64B5F6}"
                   HorizontalTextAlignment="Center"/>
            
            <!-- Metric Label -->
            <Label x:Name="LabelText"
                   FontSize="12"
                   TextColor="{AppThemeBinding Light=#757575, Dark=#B0B0B0}"
                   HorizontalTextAlignment="Center"
                   Margin="0,0,0,4"/>
            
            <!-- Trend Indicator (Optional) -->
            <HorizontalStackLayout x:Name="TrendContainer"
                                   HorizontalOptions="Center"
                                   Spacing="4"
                                   IsVisible="False">
                <Label x:Name="TrendLabel"
                       FontSize="11"
                       FontAttributes="Bold"/>
            </HorizontalStackLayout>
        </VerticalStackLayout>
    </Border>
</ContentView>
