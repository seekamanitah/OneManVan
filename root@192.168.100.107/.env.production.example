# Production Environment Configuration
# Copy this to .env and customize for your deployment

# Database Configuration
SA_PASSWORD=ChangeThisPassword123!
DB_NAME=TradeFlowFSM

# Application Environment
ASPNET_ENVIRONMENT=Production

# Ports (change if needed)
WEB_PORT=5000
HTTPS_PORT=5001
SQL_PORT=1433

# Backup Schedule (for cron)
BACKUP_SCHEDULE=0 2 * * *  # Daily at 2 AM

# Network Configuration
DOCKER_NETWORK=tradeflow-net

# Security Notes:
# 1. CHANGE SA_PASSWORD immediately
# 2. Use strong passwords (minimum 12 characters, mix of upper/lower/numbers/symbols)
# 3. Never commit this file to source control
# 4. Use Docker secrets in production environment
# 5. Enable HTTPS with proper certificates
