namespace OneManVan.Shared.Models.Enums;

/// <summary>
/// Custom field data types for the no-code schema extension system.
/// </summary>
public enum CustomFieldType
{
    Text = 0,
    Number = 1,
    Decimal = 2,
    Date = 3,
    Boolean = 4,
    Enum = 5
}
